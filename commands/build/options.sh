# Generated by stubbs:add-option. Do not edit, if using stubbs.
# Created: Thu Dec 13 20:09:58 PST 2012
#
#/ usage: zookeeper:build [ --url <http://apache.osuosl.org/zookeeper>] [ --package-format <rpm>]  --package-version <> [ --package-release <1>]  --package-directory <> 

# _rerun_options_parse_ - Parse the command arguments and set option variables.
#
#     rerun_options_parse "$@"
#
# Arguments:
#
# * the command options and their arguments
#
# Notes:
# 
# * Sets shell variables for any parsed options.
# * The "-?" help argument prints command usage and will exit 2.
# * Return 0 for successful option parse.
#
rerun_options_parse() {
    
    while [ "$#" -gt 0 ]; do
        OPT="$1"
        case "$OPT" in
            --url) rerun_option_check $# ; URL=$2 ; shift ;;
            --package-format) rerun_option_check $# ; PACKAGE_FORMAT=$2 ; shift ;;
            --package-version) rerun_option_check $# ; PACKAGE_VERSION=$2 ; shift ;;
            --package-release) rerun_option_check $# ; PACKAGE_RELEASE=$2 ; shift ;;
            --package-directory) rerun_option_check $# ; PACKAGE_DIRECTORY=$2 ; shift ;;
            # help option
            -?)
                rerun_option_usage
                exit 2
                ;;
            # end of options, just arguments left
            *)
              break
        esac
        shift
    done

    # Set defaultable options.
    [ -z "$URL" ] && URL="http://apache.osuosl.org/zookeeper"
    [ -z "$PACKAGE_FORMAT" ] && PACKAGE_FORMAT="rpm"
    [ -z "$PACKAGE_RELEASE" ] && PACKAGE_RELEASE="1"
    # Check required options are set
    [ -z "$PACKAGE_VERSION" ] && { echo >&2 "missing required option: --package-version" ; return 2 ; }
    [ -z "$PACKAGE_DIRECTORY" ] && { echo >&2 "missing required option: --package-directory" ; return 2 ; }
    # If option variables are declared exportable, export them.

    #
    return 0
}


# Initialize the options variables to null.
URL=
PACKAGE_FORMAT=
PACKAGE_VERSION=
PACKAGE_RELEASE=
PACKAGE_DIRECTORY=


